#ifndef BILIST_H
#define BILIST_H

#include <cstddef>

template <class T>
struct BiList {
    T val;
    BiList<T>* next;
    BiList<T>* prev;
    
    BiList() : next(nullptr), prev(nullptr) {}
    BiList(const T& value) : val(value), next(nullptr), prev(nullptr) {}
};

#endif // BILIST_H
